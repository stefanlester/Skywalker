# Give your application a unique name (no spaces)
APP_NAME=${APP_NAME}
APP_URL=http://localhost:4000

# false for production, true for development
DEBUG=true

# the port should we listen on
PORT=4000

# the server name, e.g, www.mysite.com
SERVER_NAME=localhost

# should we use https?
SECURE=false

# database config - postgres or mysql
DATABASE_TYPE=
DATABASE_HOST=
DATABASE_PORT=
DATABASE_USER=
DATABASE_PASS=
DATABASE_NAME=
DATABASE_SSL_MODE=

# redis config
REDIS_HOST=
REDIS_PASSWORD=
REDIS_PREFIX=${APP_NAME}

# cache (currently only redis or badger)
CACHE=

# cookie seetings
COOKIE_NAME=${APP_NAME}
COOKIE_LIFETIME=1440
COOKIE_PERSIST=true
COOKIE_SECURE=false
COOKIE_DOMAIN=localhost

# session store: cookie, redis, mysql, or postgres
SESSION_TYPE=cookie

# mail settings
SMTP_HOST=
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_PORT=1025
SMTP_ENCRYPTION=
MAIL_DOMAIN=
FROM_NAME=
FROM_ADDRESS=

# mail settings for api services
MAILER_API=
MAILER_KEY=
MAILER_URL=

# template engine: go or jet
RENDERER=jet

# the encryption key; must be exactly 32 characters long
KEY=${KEY}

# remote filesystem backends. Each backend is constructed only when its gate
# variable is set: MINIO_SECRET, S3_SECRET, SFTP_HOST, WEBDAV_HOST.
#
# The MINIO backend speaks the S3 protocol and works with any S3-compatible
# store (SeaweedFS, Garage, MinIO, Cloudflare R2, ...). The skeleton's
# docker-compose bundles SeaweedFS on 127.0.0.1:9000 with dev credentials
# root/password — set MINIO_SECRET=password to enable it.
MINIO_ENDPOINT=127.0.0.1:9000
MINIO_KEY=root
MINIO_SECRET=
MINIO_USESSL=false
MINIO_REGION=us-east-1
MINIO_BUCKET=testbucket

S3_ENDPOINT=
S3_KEY=
S3_SECRET=
S3_REGION=
S3_BUCKET=

SFTP_HOST=
SFTP_USER=
SFTP_PASS=
SFTP_PORT=

WEBDAV_HOST=
WEBDAV_USER=
WEBDAV_PASS=